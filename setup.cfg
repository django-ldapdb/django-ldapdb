[bdist_wheel]
universal = true

[zest.releaser]
; semver-style versions
version-levels = 3

; Version flag location (we use __version__)
python-file-with-version = ldapdb/version.py

[distutils]
index-servers = pypi
